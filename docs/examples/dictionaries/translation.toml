schema_version = 1

[project]
id = "vn-dictionaries-clean"
source_language = "ja"
adapter = "json"
backend = "compatible"

[adapter]
input = "resources.json"

[backends.compatible]
base_url = "https://api.deepseek.com"
api_key_env = "MODEL_API_KEY"
# Configure model here or pass --model before translating.

[targets.zh-Hans]
translations = "../../../translations/ci-smoke-clean/zh-Hans"
term_sources = [{ file = "names.json" }]
